server:
  port: 8099
  servlet:
    context-path: /

spring:
  application:
    name: AgentTest
  profiles:
    active: local

langchain4j:
  community:
    dashscope:
      chat-model:
        api-key: <You API Key here>
#        model-name: qwen-vl-max  # 多模态模型
        model-name: qwen-plus  # 文本模型
        temperature: 0.7
        max-tokens: 4096
        top-p: 0.9

logging:
  level:
    com.alibaba.dashscope: DEBUG  # 查看 DashScope SDK 的请求细节
    dev.langchain4j: DEBUG